#include<stdio.h>
void main()
{
    int a,b;
    printf("Enter a & b value\n");
    scanf("%d %d",&a, &b); // user to enter the value
    printf("The addition of a & b is:%d\n", a+b); // the output for addition

    
    // when you use int a = 2 or float b = 3.0
    //the user cannot enter value as a is set to 4 by deafult by you
    // scanf is used where user can enter value 
    /* syntax:  scanf("%d", &a);
    the user can enter the value and it will be stored in "a" 
    */
   // You try for subtraction, mul, division below this 
}
